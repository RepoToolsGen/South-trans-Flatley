{
  "name": "repo-gen",
  "version": "1.0.0",
  "description": "",
  "main": "./dist",
  "scripts": {
    "build": "npm run clean && npx tsc",
    "postbuild": "cpy --flat ./src/repoConfig.json ./dist/",
    "clean": "npx rimraf dist",
    "formatter": "npx prettier --write --ignore-path .gitignore .",
    "formatter:check": "npx prettier --check --ignore-path .gitignore .",
    "start": "node ./dist/repoGen.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@faker-js/faker": "^7.6.0",
    "@types/glob": "^8.1.0",
    "@types/shelljs": "^0.8.11",
    "@types/url-parse": "^1.4.8",
    "axios": "^1.3.4",
    "shelljs": "^0.8.5"
  },
  "devDependencies": {
    "cpy": "^9.0.1",
    "cpy-cli": "^4.2.0",
    "dotenv": "^16.0.3",
    "prettier": "^2.8.0",
    "rimraf": "^3.0.2"
  }
}
